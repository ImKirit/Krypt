{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. Only core events and window handling, no file, shell or network plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
